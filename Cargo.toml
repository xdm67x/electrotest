[package]
name = "electrotest"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
thiserror = "1"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
